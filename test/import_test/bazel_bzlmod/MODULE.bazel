module(
    name = "my_module",
    version = "1.0",
)

bazel_dep(name = "ex_actor", version = "0.1")
bazel_dep(name = "rules_cc", version = "0.2.3")

# ------------------------ ex-actor archive_override(to be filled) ------------------------
